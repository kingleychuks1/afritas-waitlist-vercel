# Set automatically by Vercel once you connect a Postgres store to this
# project (Storage tab -> Create Database -> Postgres -> Connect Project).
# You only need to set this by hand for local dev without `vercel env pull`.
POSTGRES_URL=
