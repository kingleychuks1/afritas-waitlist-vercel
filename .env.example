# Vercel sets one of these automatically once your Neon Postgres storage is
# connected to this project (Project -> Storage -> your Neon DB -> Connect
# Project). lib/db.js checks them in this order. You only need to set one
# by hand for local dev if `vercel env pull` isn't an option.
DATABASE_URL=
POSTGRES_URL=
POSTGRES_PRISMA_URL=
